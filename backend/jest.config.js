module.exports = {
    testEnvironment: 'node',
    testMatch: ['**/tests/**/*.test.js'],
    verbose: true,
    forceExit: true,
    // setupFilesAfterEnv: ['./tests/setup.js'], // Uncomment if we need global setup
};
